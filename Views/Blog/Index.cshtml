@model IEnumerable<BlogPostApp.Models.Post>

<h2>Blog Posts</h2>
<ul>
    @foreach (var post in Model)
    {
        <li>
            <a asp-action="Details" asp-route-id="@post.Id">@post.Title</a>
        </li>
    }
</ul>